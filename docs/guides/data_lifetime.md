数据的生命周期决定它放哪里**。

生命周期 | 放哪里 | 例子 |
|---------|-------|------|
| **单帧** | 临时内存 / 栈变量 | SimRegion、渲染列表、碰撞结果 |
| **跨帧但可重算** | 临时内存（缓存） | 路径规划结果、可见性计算 |
| **永久** | GameState | entity 数据、玩家存档、世界索引


原则**：
- 能局部就局部
- 能临时就临时
- 只有必须跨帧保留的才放 GameState

这样内存使用更清晰，也更容易调试——你知道 GameState 里的东西是"真正的状态"，不是临时计算结果。
